name: food_recognizer_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.4

dependencies:
  camera: ^0.12.0+1
  envied: ^1.3.4
  firebase_core: ^4.6.0
  firebase_ml_model_downloader: ^0.4.1
  flutter:
    sdk: flutter
  google_generative_ai: ^0.4.7
  http: ^1.6.0
  image: ^4.8.0
  image_cropper: ^12.2.0
  image_picker: ^1.2.1
  provider: ^6.1.5+1
  tflite_flutter: ^0.12.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.13.1
  envied_generator: ^1.3.4

flutter:
  uses-material-design: true
  assets:
    - assets/